/** Shared shell for cards / modals (zinc-900 panel). */
export const panelClass =
  "overflow-hidden rounded-sm border border-zinc-800 bg-zinc-900 shadow-[0_1px_rgba(255,255,255,0.05)_inset]";

/** Top bar inside a panel (title row). */
export const panelHeaderClass =
  "flex items-center justify-between border-zinc-900 border-b bg-zinc-800/80 px-3 py-2";
